# Normalize all text files to LF in the repository.
* text=auto eol=lf

# Explicitly binary (never touch line endings)
*.png binary
*.webp binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
